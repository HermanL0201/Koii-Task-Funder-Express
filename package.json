{
  "name": "game-room-api",
  "version": "1.0.0",
  "description": "Game Room API for multiplayer games",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "vitest": "^0.32.4"
  }
}