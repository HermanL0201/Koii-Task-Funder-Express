{
  "name": "sephora-api",
  "version": "1.0.0",
  "description": "Sephora API with secure authentication",
  "main": "dist/index.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "bcrypt": "^5.1.1"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "vitest": "^1.2.2"
  }
}