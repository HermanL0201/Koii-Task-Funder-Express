{
  "name": "game-room-api",
  "version": "1.0.0",
  "description": "Game Room API for multiplayer platform",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "devDependencies": {
    "vitest": "^0.34.6",
    "mongoose": "^7.5.3",
    "express": "^4.18.2"
  }
}